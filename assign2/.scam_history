(define (Stack) (define store nil) (define (push x) (define store (cons x store))) (define (pop) (define tmp (car store)) (define store (cdr store)) tmp) (define (empty?) (eq? store nil)) this)
(define s (Stack))
((s (quote push)) 3)
((s (quote pop)))
(define (Stack) (define store nil) (define (push x) (define stor (cons x store))) (define (pop) (define tmp (car store)) (define store (cdr store)) tmp) (define (empty?) (eq? store nil)) this)
(quote )
inspect
(quote )
(inspect ((quote )))
(inspect (quote))
(inspect (quote ))
